name: jwt_auth_plus_example
description: Demonstrates how to use jwt_auth_plus to decode and validate tokens.
publish_to: 'none'

environment:
  sdk: ^3.5.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  jwt_auth_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true